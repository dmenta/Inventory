@model MiInventario.Models.DifferenceViewModel
@{
  ViewBag.Title = "Difference";
}

<h2>Difference</h2>
<h3>@MiInventario.Utils.GetUsernamFromEmail(Model.UsuarioA) - @MiInventario.Utils.GetUsernamFromEmail(@Model.UsuarioB)</h3>
@foreach (MiInventario.Models.GroupDifferenceViewModel group in Model.Groups) {
  foreach (MiInventario.Models.TypeDifferenceViewModel type in group.Types) {
    @Html.Partial("_ShowItemsDifference", type.Items)
  }
}
<div class="clearfix"></div>
<div>
  <p class="bottomLinks">
    @Html.ActionLink("Back to Inventory", "Index")
  </p>
</div>
