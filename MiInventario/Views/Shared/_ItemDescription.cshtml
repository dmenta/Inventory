@model MiInventario.Models.ItemViewModel
@if (Model.CurrentItem.Level > 0) {
  <span class="@string.Format("nivel{0}", Model.CurrentItem.Level)">@string.Format("L{0}", Model.CurrentItem.Level)</span>
}
<span class="@string.Format("rareza{0}", Model.CurrentItem.Rarity)">@Model.CurrentItem.Nombre</span>
